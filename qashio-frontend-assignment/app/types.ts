import type {
  Transaction,
  Category,
  CreateTransactionDto,
  UpdateTransactionDto,
  CreateCategoryDto,
} from '@/lib/api';

export type {
  Transaction,
  Category,
  CreateTransactionDto,
  UpdateTransactionDto,
  CreateCategoryDto,
};

export type TransactionFormData = CreateTransactionDto; 